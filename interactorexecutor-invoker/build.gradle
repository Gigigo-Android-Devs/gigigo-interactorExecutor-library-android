apply plugin: 'java'

apply plugin: 'maven'

group = 'com.github.Gigigo-Android-Devs'

dependencies {
  compile fileTree(dir: 'libs', include: ['*.jar'])
  compile rootProject.ext.modules.interactorExecutorPresentation
}

sourceCompatibility = "1.7"
targetCompatibility = "1.7"
